(define-module (engstrand features nix))

; TODO: Add packages
;      (if nix? (specification->package "nix")))

; TODO: Add services
;       (if nix?
;           (list
;             (service nix-service-type))
;           '())
